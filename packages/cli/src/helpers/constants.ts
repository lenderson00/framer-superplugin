import pkg from '../../package.json';

export const commandName = pkg.name;
export const projectName = 'Framer Superplugins CLI';
export const repoUrl = pkg.repository.url;
export const version = pkg.version;