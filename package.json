{
  "name": "framer-superplugin-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@framerplugin/config": "workspace:*",
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "turbo": "latest",
    "typescript": "^5.0.0"
  },
  "packageManager": "bun@1.0.0",
  "workspaces": [
    "packages/*",
    "packages/examples/*"
  ]
}
